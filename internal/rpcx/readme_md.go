package rpcx

var readmeMd = `
# {{ .Name }}

## make 命令

1. 代码生成 ` + "`" + `make codegen` + "`" + `
2. 编译 ` + "`" + `make build` + "`" + `
3. 制作镜像 ` + "`" + `make image` + "`" + `
4. 清理 ` + "`" + `make clean` + "`" + `
`
